{
  "name": "releasetest",
  "version": "6.4.3",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "private": false,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "version": "npx changeset version && node scripts/sync-version.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.29.6"
  }
}
